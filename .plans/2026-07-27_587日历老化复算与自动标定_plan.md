# 587Ah日历老化复算与自动标定 Plan

## 基于 Spec

`2026-07-27_587日历老化复算与自动标定_spec.md`

## 阶段A：矩阵复算

1. [x] 将任务脚本参数别名切换为 `587calander`。
2. [x] 运行0/25/50℃、1个月smoke并检查基准容量、保持率和恢复率。
3. [x] 新建full run，完成11温度×14个月，共154个独立算例。
4. [x] 生成CSV、Excel、14个月汇总和图表。
5. [x] 将验证结果发布到任务包并更新复现说明。

## 阶段B：自动标定工作流

1. [x] 在 `BatteryProject/src/` 增加通用表格型敏感性/标定编排模块。
2. [x] 实现协议校验、baseline、OAT、响应相关性、优化、留出验证和缓存。
3. [x] 增加单元测试。
4. [x] 在 `BatteryProject/examples/` 增加587日历老化案例与配置。
5. [x] 输出本次案例的机器可读结果和使用说明。

## 验证标准

- 新矩阵 `run_status.csv` 为154个completed/reused且无failed。
- 矩阵结果覆盖11个温度和14个月，无重复和缺失。
- 工作簿关键表与CSV数值一致，无公式错误，视觉检查可读。
- 标定模块测试覆盖敏感性排序、低敏感参数过滤、相关性和留出指标。
- `flake8 BatteryProject/src/` 和 `pytest BatteryProject/tests/` 全部通过。
- 587案例能够通过单条命令复用缓存并重新生成报告。

## 变更记录

- 2026-07-27：用户批准按“矩阵复算＋固定化标定流程”两阶段实施。
