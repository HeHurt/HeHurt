# F413 部门仿真秘籍模板标准

## 唯一真源

| 项目 | 规定 |
|---|---|
| 文件 | `E:\Downloads\HC_TS_ISC_F413_2025《基于PyBaMM计算电芯不同SOH下能效、功率性能、DCR的方法制定》.docx` |
| 参考 SHA-256 | `5dc576fd7f7f93c6900496318f59c2ae56e4632af2f25a061fed23a4bb16a483` |
| 使用方式 | 每个任务从此文件复制一份后编辑；原文件只读保存。 |

若该文件内容更新、哈希变化或用户指定新的部门模板，以用户确认后的模板为真源，并在任务说明中重新记录哈希。

## 必须继承的版式

- A4 纵向版面；模板现有页边距、封面、页眉横线、页码和页眉页脚均原样保留。
- 延续克制的黑色工程文档视觉：模板的字体、字号、段前段后、首行、表格线、图题和表题不可自行替换。
- 使用模板的标题样式和多级编号，使目录可自动更新；不要手敲目录或页码。
- 保留“封面 → 编制说明 → 目录 → 正文方法制定 → 结论/适用边界”的总体顺序。正文标题名称可按课题替换，但不改变模板的层级和叙事节奏。
- 正文图表紧跟其解释文字，图题置于图下、表题置于表上；编号跟随模板章节规则，不复用旧编号。

## 内容可信度标记

| 内容类型 | 允许写法 |
|---|---|
| 已运行且可复现 | 给出 run/脚本、工况、指标和图源。 |
| COMSOL 模型迭代 | 给出 baseline 与任务版本、节点/参数改动、单位、保存路径/哈希及求解或后处理证据。 |
| GPT 多轮对话决策 | 给出对话轮次或演进台账、假设和实际落盘改动；未运行时只能写为设计或待验证。 |
| 已实现但未运行 | 说明代码链路与预期观测，明确标注“待运行”。 |
| 机理示意 | 标注“机理示意”或“流程示意”，不伪装为结果。 |
| 缺少实验或模型输入 | 在正文与交付说明列为“待确认/待补图/待验证”。 |
