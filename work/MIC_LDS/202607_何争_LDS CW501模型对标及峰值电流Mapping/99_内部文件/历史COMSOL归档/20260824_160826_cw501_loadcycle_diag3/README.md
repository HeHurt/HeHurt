# CW501 Load Cycle 求解器序列错配诊断

本目录记录对当前 `.mph` 的只读回读。新失败使用 `sol15/t1`，而用户调整的稳定性设置保存在 `sol1/t1`。

- `current_state.json`：当前 Load Cycle 与各求解器的实际属性。
- `error_report.txt`：原因与界面处理建议。
- `metrics.csv`：`sol1` 与 `sol15` 的设置对比。

未修改源模型，未执行新求解。

