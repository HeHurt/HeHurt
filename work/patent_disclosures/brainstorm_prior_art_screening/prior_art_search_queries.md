# 储能电池专利头脑风暴方向 - 度衍复核检索式

说明：以下检索式用于度衍/中文专利库复核。未申请前建议只输入脱敏关键词，不上传完整交底书、实验数据或内部模型参数。分类号可作为筛选条件，不宜一开始限制过死。

## 1. 基于数字孪生的储能电芯动态功率限值方法

中文检索式：
`(功率限值 OR 状态功率 OR SOP OR 峰值功率 OR 持续功率) AND (数字孪生 OR 电化学模型 OR 等效电路模型 OR 机器学习模型) AND (SOC OR SOH OR 温度 OR 内阻 OR DCR) AND (电池 OR 储能 OR 锂电池)`

英文关键词：
`battery state of power`, `power capability`, `dynamic power limit`, `electrochemical model`, `digital twin`, `SOC temperature SOH impedance`

分类号：
`G01R31/367`、`G01R31/392`、`H01M10/48`、`H02J7/00`、`G06F`、`G06N`

## 2. 储能电芯电解液干涸早期诊断方法

中文检索式：
`(电解液干涸 OR 电解液耗尽 OR 缺液 OR 贫液 OR 浸润不足 OR 电解液枯竭) AND (残差 OR 极化 OR DCR OR 内阻 OR 温升 OR 弛豫) AND (诊断 OR 识别 OR 预警 OR 评分) AND (储能 OR 锂电池)`

英文关键词：
`electrolyte dry-out`, `electrolyte depletion`, `electrolyte starvation`, `voltage residual`, `polarization residual`, `pulse temperature rise`, `aging diagnosis`

分类号：
`G01R31/367`、`G01R31/392`、`G01R31/3842`、`H01M10/48`、`G06F`

## 3. 大容量储能电芯的分层 DCR 测试与寿命预测方法

中文检索式：
`(DCR OR DCIR OR 直流内阻 OR 直流阻抗) AND (脉冲 OR 多脉冲 OR 脉冲宽度 OR 欧姆 OR 电荷转移 OR 扩散) AND (SOH OR 寿命 OR 功率衰减 OR 功率预测) AND (电池 OR 储能)`

英文关键词：
`direct current resistance`, `DCIR`, `DCR`, `pulse duration`, `ohmic resistance`, `charge transfer resistance`, `diffusion resistance`, `power fade`

分类号：
`G01R31/389`、`G01R31/392`、`G01R31/367`、`H01M10/48`

## 4. 基于模型残差的储能电芯异常老化识别方法

中文检索式：
`(模型残差 OR 电压残差 OR 温度残差 OR 输出残差 OR 模型预测) AND (故障诊断 OR 异常检测 OR 老化识别 OR 多故障) AND (锂电池 OR 储能 OR BMS)`

英文关键词：
`battery model residual`, `voltage residual`, `model-based fault diagnosis`, `electrochemical model residual`, `aging mode detection`, `multi-fault diagnosis`

分类号：
`G01R31/367`、`G01R31/392`、`G01R31/3842`、`G06F`、`G06N`

## 5. 储能系统簇级 SOC/SOH 均衡控制方法

中文检索式：
`(电池簇 OR 储能簇 OR PACK OR 模组) AND (均衡 OR 功率分配 OR 电流分配 OR SOC 均衡) AND (SOH OR 健康状态 OR 容量 OR 内阻 OR 老化) AND (储能系统)`

英文关键词：
`battery cluster balancing`, `SOC equalization`, `SOH`, `power allocation`, `current sharing`, `energy storage system`

分类号：
`H02J7/00`、`H02J3/32`、`H01M10/48`、`G01R31/392`

## 6. 低温储能电池防锂析出的充电控制方法

中文检索式：
`(低温 OR 快充 OR 充电) AND (锂析出 OR 析锂 OR 镀锂 OR 负极电位 OR 负极过电位) AND (控制 OR 限流 OR 预热 OR 防止) AND (锂电池 OR 储能)`

英文关键词：
`lithium plating`, `low temperature charging`, `anode potential`, `negative electrode potential`, `charging current control`, `fast charging`

分类号：
`H02J7/00`、`H01M10/44`、`H01M10/48`、`G01R31/367`

## 7. 储能电芯热失控前兆识别方法

中文检索式：
`(热失控 OR 热 runaway OR 火灾 OR 早期预警) AND (电压 OR 温度 OR 内阻 OR 气体 OR 压力 OR 应变 OR 自放电) AND (多源融合 OR 评分 OR 分级预警) AND (电池 OR 储能)`

英文关键词：
`thermal runaway early warning`, `battery safety`, `voltage temperature resistance gas pressure strain`, `multi-sensor fusion`, `state of safety`

分类号：
`H01M10/48`、`H01M10/613`、`H01M10/625`、`G01R31/36`、`G01K`、`G01N`

## 8. 基于仿真的储能电池实验工况自动生成方法

中文检索式：
`(实验工况 OR 测试工况 OR 老化工况 OR 加速老化 OR 测试 profile OR 工况生成) AND (真实工况 OR 使用数据 OR 调频 OR 削峰填谷 OR 光储) AND (仿真 OR 模型 OR 老化路径) AND (电池 OR 储能)`

英文关键词：
`battery test profile generation`, `accelerated aging profile`, `real-world usage profile`, `duty cycle`, `aging path`, `simulation`

分类号：
`G01R31/367`、`G01R31/392`、`G06F`、`G06N`、`H01M10/48`

## 9. 储能电芯 OCV 曲线在线校准方法

中文检索式：
`(OCV OR 开路电压 OR SOC-OCV OR OCV-SOC) AND (在线校准 OR 曲线校准 OR SOC 校准 OR 滞回) AND (静置 OR 低倍率 OR BMS) AND (锂电池 OR 储能)`

英文关键词：
`OCV-SOC calibration`, `online OCV calibration`, `SOC correction`, `open circuit voltage`, `hysteresis coefficient`, `resting voltage`

分类号：
`G01R31/367`、`G01R31/3842`、`G01R31/392`、`H01M10/48`

## 10. 基于容量-功率联合衰减的储能电站退役分选方法

中文检索式：
`(退役电池 OR 梯次利用 OR 退役筛选 OR 分选 OR 重组) AND (容量 OR 内阻 OR DCR OR 功率能力 OR 温升 OR 能效) AND (SOH OR 衰减 OR 分级) AND (储能 OR 锂电池)`

英文关键词：
`retired battery sorting`, `second life battery`, `capacity internal resistance`, `DCR`, `power capability`, `battery regrouping`, `echelon utilization`

分类号：
`G01R31/392`、`G01R31/389`、`G01R31/367`、`H01M10/48`、`B07C`
