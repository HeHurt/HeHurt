# 扣电与软包力电耦合

- 任务编号：`202609_何争_扣电与软包力电耦合V2`
- 电芯/体系：`AI_virtual_cell`
- 负责人：何争
- 版本：V2
- 状态：正确极性、参数化堆叠及右侧密封闭合已重建；稳态力学通过，瞬态求解尚未验收通过。

## 本版目标

1. 以 `CR2430_axisymmetric_v9_RCC_force.mph` 为正确极性与材料属性参考，纠正旧模型正负极壳体方向。
2. 使用 `coin.mphbin` 对照几何尺寸，在 COMSOL 中重建可参数化的二维轴对称原生几何。
3. 正极厚度 `t_ca` 或负极厚度 `t_an` 改变时，上方各层、垫片及负极壳体自动平移。
4. 保留扣电 V9 的 LFP/石墨电化学、力学、压力—孔隙率反馈及嵌锂呼吸设置，并重新绑定到正确域。

## 验收状态

- 通过：右侧 PC 密封圈已参数化延伸至正极壳内壁；回读拓扑为 42 个点、53 条边界、11 个域。
- 通过：正负极、隔膜、集流体、壳体、端子及力学边界选择回读一致。
- 通过：网格共 1970 个单元，电芯五层厚度方向采用映射网格。
- 通过：补齐 `mech_steel` 与 `mech_pp` 的域选择后，外力稳态研究烟雾计算成功。
- 未通过：120 s 全耦合瞬态烟雾计算仍在 `sol3/t1` 报错；正式完整循环未运行。
