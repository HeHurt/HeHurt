# 复现说明

1. 环境检查与通用运行入口保留在项目 `COMSOL/scripts/check_env.m`、`run_case.m` 和 `postprocess_case.m`。
2. 演示模型位于 `02_模型/`，工具链汇报位于 `01_仿真报告/`。
3. 历史运行与回归测试位于 `99_内部文件/历史COMSOL归档/runs/`，对应结果位于其 `results/` 子目录。
4. 后续工具链任务应在本任务包 `99_内部文件/运行记录/` 内增加记录；仅在实际失败时生成错误报告。
