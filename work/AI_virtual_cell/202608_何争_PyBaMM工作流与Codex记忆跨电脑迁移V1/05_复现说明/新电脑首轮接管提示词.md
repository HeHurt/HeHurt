# 新电脑首轮接管提示词

```text
你正在接管从旧电脑迁移来的 Hithium PyBaMM 电池仿真工作区和 Codex 本地长期记忆。

本轮只做只读迁移审计。在我确认前，不修改代码、参数、数据、Notebook、AGENTS.md 或记忆文件；不安装或升级依赖；不运行长时间仿真。

请依次完成：

1. 确认当前 Git 根目录、实际物理路径、branch、HEAD、dirty 状态以及 junction/symlink。
2. 完整读取根目录 AGENTS.md，并按当前范围读取 BatteryProject/src、params、work 下适用的嵌套 AGENTS.md。
3. 确认 CODEX_HOME 和 memories 配置：features.memories、memories.generate_memories、memories.use_memories 均为 true。
4. 读取 <CODEX_HOME>/memories/memory_summary.md 和 MEMORY.md；只继续读取其中直接指向且与 Hithium、BatteryProject、PyBaMM、参数、老化、实验对标、任务交付、DLP 有关的 rollout summaries 和 memory skills。
5. 检查项目 skills 是否可发现，重点确认 hithium-pybamm-debugging、pybamm-debug、sim-conclusion。
6. 搜索并报告旧绝对路径、失效 junction、缺失数据和本机专属依赖，重点检查 C:\HithiumSSD\hithium、D:\Users\hez\Desktop\hithium、C:\Users\hez。
7. 核对 Python、PyBaMM、CasADi、IDAKLU、NumPy、SciPy、pandas、matplotlib、openpyxl、scienceplots、liionpack 的实际版本。
8. 核对 datasets.json、data_raw、data_processed、BatteryProject/output/runs、work、params 是否完整；不要重扫 registry。
9. 输出迁移验收报告，分为：已验证正常、已迁移未运行验证、缺失或路径失效、DLP/权限风险、建议的最小 smoke test。

继承原则：
- 全程中文答复，技术标识符保留英文。
- 以本地文件、运行日志和真实数据为证据。
- 不把未验证假设称为根因。
- 每次换温度重新创建 pybamm.ParameterValues("OKane2022") 再 params.update()。
- 保持参数文件、datasets registry、任务包和 BatteryProject/output/runs 的路径契约。
- 先完成原版本等价复现，再讨论升级或重构。
```
