# 无效诊断模型说明

这里的 V9 文件来自一次将二维软包截面误判为三维模型的诊断尝试，错误地把厚度方向从 `y` 改为 `z`。实际 V8 是二维模型，厚度方向为 `y`，因此这些 V9 文件不得用于物理结论或正式交付，仅作为错误复盘留痕。

正式结果仍以 `02_模型/软包_V8_0p13Ah_0p5C_100N完整耦合.mph` 和新增的 `02_模型/软包_V8_0p13Ah_0p5C_100N无呼吸完整循环.mph` 为准。
